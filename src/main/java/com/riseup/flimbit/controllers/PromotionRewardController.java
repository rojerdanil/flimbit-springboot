package com.riseup.flimbit.controllers;

import com.riseup.flimbit.entity.PromotionReward;
import com.riseup.flimbit.service.PromotionRewardService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/*@RestController
@RequestMapping("/promotion-rewards/")*/
public class PromotionRewardController {

	/*
	 * @Autowired private PromotionRewardService promotionRewardService;
	 * 
	 * @GetMapping("/type/{promotionTypeId}") public List<PromotionReward>
	 * getRewards(@PathVariable Long promotionTypeId) { return
	 * promotionRewardService.getRewardsByPromotionType(promotionTypeId); }
	 * 
	 * @PostMapping("/create") public PromotionReward createReward(@RequestBody
	 * PromotionReward reward) { return promotionRewardService.createReward(reward);
	 * }
	 */
}
